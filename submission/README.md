# TopologyProof submission evidence

This directory contains a real completed webhook analysis and an auditable six-criterion comparison. baseline-webhook.json is the deterministic diff-only output; actionable-quality.json scores both outputs. The trajectories directory contains copied real request, findings, JSONL trajectory, and Markdown report artifacts. Observable stages are recorded, not private chain-of-thought. Runtime is NOT EXECUTED in M1.
