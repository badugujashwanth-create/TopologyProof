["finding_id='TP-001' title='Process-local webhook deduplication guards a durable payment side effect.' category=<TopologyDimension.STATE_LOCALITY: 'state_locality'> severity=<Severity.HIGH: 'high'> confidence=0.94 deployment_assumption='Equivalent deliveries observe shared deduplication state across processes.' topology_dimensions=(<TopologyDimension.REPLICA_COUNT: 'replica_count'>, <TopologyDimension.REQUEST_ROUTING: 'request_routing'>, <TopologyDimension.STATE_LOCALITY: 'state_locality'>, <TopologyDimension.RESTART_RECOVERY: 'restart_recovery'>) evidence=(EvidenceLocation(path=PurePosixPath('app/main.py'), line=8, line_end=8, symbol='processed_events', commit_id='383d8fc52d68565dbef83a8b84975fe956bb5c55', excerpt='processed_events: set[str] = set()'),) correctness_property='One event identifier produces at most one durable payment record.' predicted_failure='Duplicate durable payment records when requests reach separate workers.' verification_recommendation=VerificationRecommendation(worth_running=True, summary='Send the same event ID to separate workers and compare durable records.', topology_dimensions=(<TopologyDimension.REPLICA_COUNT: 'replica_count'>, <TopologyDimension.REQUEST_ROUTING: 'request_routing'>, <TopologyDimension.STATE_LOCALITY: 'state_locality'>, <TopologyDimension.RESTART_RECOVERY: 'restart_recovery'>), property_assertion='One event identifier produces at most one durable payment record.') verdict=<FindingVerdict.HIGH_RISK: 'high-risk'> limitations=()"]